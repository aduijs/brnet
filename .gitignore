# Build artifacts
node_modules/

# Local working clutter (production only)
temporary screenshots/
.claude/

# Backup files (timestamped .bak.* files left by GIBIL/tools)
*.bak
*.bak.*

# Archives (these are not source — owner generates tarballs on production)
*.tar.gz
*.tgz
*.zip

# OS junk
.DS_Store
Thumbs.db

# Stray screenshots at repo root (workflow uses ./temporary screenshots/)
/screenshot-*.png

# Editor / IDE
.vscode/
.idea/
*.swp
